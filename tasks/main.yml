---
- name: Install the sudo package
  package:
    name: sudo
    state: present
  tags:
    - sudoers

- name: Deploy the sudoers file
  template:
    src: sudoers.j2
    dest: /etc/sudoers
    owner: root
    group: root
    mode: 0440
    validate: visudo -cf %s
  tags:
    - sudoers
